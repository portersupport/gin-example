version: v1
resources:
- name: web
  source:
    name: web
  config:
    build:
      method: docker
      context: .
      dockerfile: ./subpath/Dockerfile
    values:
      container:
        command: web
        port: 8080
        env:
          normal:
            TESTING: 'true'
            TESTING2: 'TRUE'
            TESTING3: 'false'
